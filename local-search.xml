<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>CommonJS</title>
    <link href="/2020/01/15/CommonJS/"/>
    <url>/2020/01/15/CommonJS/</url>
    
    <content type="html"><![CDATA[<h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>在了解CommonJS和AMD/CMD规范前，还是需要先来简单地了解下什么是模块化，模块化开发？</p><blockquote><p>模块化是指在解决某一个复杂问题或者一系列的杂糅问题时，依照一种分类的思维把问题进行系统性的分解以之处理。模块化是一种处理复杂系统分解为代码结构更合理，可维护性更高的可管理的模块的方式。</p></blockquote><blockquote><p>可以想象一个巨大的系统代码，被整合优化分割成逻辑性很强的模块时，对于软件是一种何等意义的存在。对于软件行业来说：解耦软件系统的复杂性，使得不管多么大的系统，也可以将管理，开发，维护变得“有理可循”。</p></blockquote><blockquote><p>还有一些对于模块化一些专业的定义为：模块化是软件系统的属性，这个系统被分解为一组高内聚，低耦合的模块。那么在理想状态下我们只需要完成自己部分的核心业务逻辑代码，其他方面的依赖可以通过直接加载被人已经写好模块进行使用即可。</p></blockquote><p>首先，既然是模块化设计，那么作为一个模块化系统所必须的能力：</p><ol><li>定义封装的模块。</li><li>定义新模块对其他模块的依赖。</li><li>可对其他模块的引入支持。</li></ol><p>好了，思想有了，那么总要有点什么来建立一个模块化的规范制度吧，不然各式各样的模块加载方式只会将局搅得更为混乱。那么在JavaScript中出现了一些非传统模块开发方式的规范 CommonJS的模块规范，AMD（Asynchronous Module Definition），CMD（Common Module Definition）等。</p><h2 id="CommonJS"><a href="#CommonJS" class="headerlink" title="CommonJS"></a>CommonJS</h2><p>CommonJS作为一套JS的规范，它的终极目标是提供一个类似Python，Ruby和Java标准库。这样的话，开发者可以使用CommonJS API编写应用程序，然后这些应用可以运行在不同的JavaScript解释器和不同的主机环境中。在兼容CommonJS的系统中，你可以使用JavaScript程序开发。</p><p>CommonJS定义的模块分为:</p><ul><li>require：该方法读取一个文件并执行，最后返回文件内部的exports对象</li><li>exports：用于导出当前模块的方法或变量，唯一的导出口</li><li>module：模块本身</li></ul><p>来看一个直观的例子：</p><pre><code class="javascript">// moduleA.jsmodule.exports = function( value ){    return value * 2;}</code></pre><pre><code class="javascript">// moduleB.jsvar multiplyBy2 = require(&#39;./moduleA&#39;);var result = multiplyBy2(4);</code></pre><p>CommonJS 加载模块是<code>同步</code>的，所以只有加载完成才能执行后面的操作。像Node.js主要用于服务器的编程，加载的模块文件一般都已经存在本地硬盘，所以加载起来比较快，不用考虑异步加载的方式，所以CommonJS规范比较适用。但如果是浏览器环境，要从服务器加载模块，这是就必须采用异步模式。所以就有了 AMD CMD 解决方案。</p><h2 id="AMD"><a href="#AMD" class="headerlink" title="AMD"></a>AMD</h2><p>Asynchronous Module Definition，用白话文讲就是 异步模块定义，对于 JSer 来说，异步是再也熟悉不过的词了，所有的模块将被异步加载，模块加载不影响后面语句运行。所有依赖某些模块的语句均放置在回调函数中。</p><p>AMD规范定义了一个自由变量或者说是全局变量 define 的函数。</p><pre><code class="javascript">define(id?: String, dependencies?: String[], factory: Function|Object);</code></pre><ul><li><h3 id="id-名字"><a href="#id-名字" class="headerlink" title="id:名字"></a>id:名字</h3><p>第一个参数，id，是个字符串。它指的是定义中模块的名字，这个参数是可选的。如果没有提供该参数，模块的名字应该默认为模块加载器请求的指定脚本的名字。如果提供了该参数，模块名必须是“顶级”的和绝对的（不允许相对名字）。</p><p>模块名用来唯一标识定义中模块，它们同样在依赖数组中使用。AMD的模块名规范是CommonJS模块名规范的超集。引用如下：</p><ol><li>模块名是由一个或多个单词以正斜杠为分隔符拼接成的字符串</li><li>单词须为驼峰形式，或者”.”，”..”</li><li>模块名不允许文件扩展名的形式，如”.js”</li><li>模块名可以为 “相对的” 或 “顶级的”。如果首字符为”.”或”..”则为”相对的”模块名</li><li>顶级的模块名从根命名空间的概念模块解析</li><li>相对的模块名从 “require” 书写和调用的模块解析</li></ol><p>上文引用的CommonJS模块id属性常被用于JavaScript模块。</p><p>相对模块名解析示例：</p><pre><code>如果模块 &quot;a/b/c&quot; 请求 &quot;../d&quot;, 则解析为&quot;a/d&quot;如果模块 &quot;a/b/c&quot; 请求 &quot;./e&quot;, 则解析为&quot;a/b/e&quot;</code></pre></li><li><h3 id="dependencies-依赖"><a href="#dependencies-依赖" class="headerlink" title="dependencies:依赖"></a>dependencies:依赖</h3><p>第二个参数，dependencies，是个定义中模块所依赖模块的数组。依赖模块必须根据模块的工厂方法优先级执行，并且执行的结果应该按照依赖数组中的位置顺序以参数的形式传入（定义中模块的）工厂方法中。</p><p>依赖的模块名如果是相对的，应该解析为相对定义中的模块。换句话来说，相对名解析为相对于模块的名字，并非相对于寻找该模块的名字的路径。</p><p>本规范定义了三种特殊的依赖关键字。如果”require”,”exports”, 或 “module”出现在依赖列表中，参数应该按照CommonJS模块规范自由变量去解析。</p><p>依赖参数是可选的，如果忽略此参数，它应该默认为[“require”, “exports”, “module”]。然而，如果工厂方法的形参个数小于3，加载器会选择以函数指定的参数个数调用工厂方法。</p></li><li><h3 id="factory-工厂方法"><a href="#factory-工厂方法" class="headerlink" title="factory:工厂方法"></a>factory:工厂方法</h3><p>第三个参数，factory，为模块初始化要执行的函数或对象。如果为函数，它应该只被执行一次。如果是对象，此对象应该为模块的输出值。</p><p>如果工厂方法返回一个值（对象，函数，或任意强制类型转换为true的值），应该为设置为模块的输出值。</p></li></ul><p><strong>举个例子</strong></p><p>创建一个名为”alpha”的模块，使用了require，exports，和名为”beta”的模块:</p><pre><code class="javascript">define(&quot;alpha&quot;, [&quot;require&quot;, &quot;exports&quot;, &quot;beta&quot;], function (require, exports, beta) {       exports.verb = function() {           return beta.verb();           //Or:           return require(&quot;beta&quot;).verb();       }   });</code></pre><p>一个返回对象的匿名模块：</p><pre><code class="javascript">define([&quot;alpha&quot;], function (alpha) {       return {         verb: function(){           return alpha.verb() + 2;         }       };   });</code></pre><p>一个没有依赖性的模块可以直接定义对象：</p><pre><code class="javascript">define({     add: function(x, y){       return x + y;     }   });</code></pre><p>一个使用了简单CommonJS转换的模块定义：</p><pre><code class="javascript">define(function (require, exports, module) {     var a = require(&#39;a&#39;),         b = require(&#39;b&#39;);     exports.action = function () {};   });</code></pre><h2 id="CMD"><a href="#CMD" class="headerlink" title="CMD"></a>CMD</h2><p>在 CMD 规范中，一个模块就是一个文件。<code>define</code> 是一个全局函数，用来定义模块。代码的书写格式如下：</p><pre><code class="javascript">define(id?, deps?, factory)</code></pre><p>字符串 <code>id</code> 表示模块标识，数组 <code>deps</code> 是模块依赖, <code>factory</code> 可以是一个函数，也可以是一个对象或字符串。</p><p><code>id</code> 和 <code>deps</code> 参数可以省略。省略时，可以通过构建工具自动生成。</p><pre><code class="javascript">factory` 为对象、字符串时，表示模块的接口就是该对象、字符串。`factory` 为函数时，表示是模块的构造方法。执行该构造方法，可以得到模块向外提供的接口。factory 方法在执行时，默认会传入三个参数：`require`、`exports` 和 `module//省略 id 和 depsdefine(function(require, exports, module) {  // 模块代码});//不省略：字符串 id 表示模块标识，数组 deps 是模块依赖。define(&#39;hello&#39;, [&#39;jquery&#39;], function(require, exports, module) {  // 模块代码});</code></pre><ul><li><p>require: require 是 factory 函数的第一个参数。require 是一个方法，接受 模块标识 作为唯一参数，用来获取其他模块提供的接口。</p><ol><li><p>require.async <code>require.async(id, callback?)</code> : require.async 方法用来在模块内部异步加载模块，并在加载完成后执行指定回调。callback 参数可选。</p><pre><code class="javascript">define(function(require, exports, module) {  // 异步加载一个模块，在加载完成时，执行回调  require.async(&#39;./b&#39;, function(b) {    b.doSomething();  });  // 异步加载多个模块，在加载完成时，执行回调  require.async([&#39;./c&#39;, &#39;./d&#39;], function(c, d) {    c.doSomething();    d.doSomething();  });});</code></pre><p><strong><em>注意</em></strong>：require 是同步往下执行，require.async 则是异步回调执行。require.async 一般用来加载可延迟异步加载的模块。</p></li><li><p>require.resolve<code>require.resolve(id)</code>: 使用模块系统内部的路径解析机制来解析并返回模块路径。该函数不会加载模块，只返回解析后的绝对路径。这可以用来获取模块路径，一般用在插件环境或需动态拼接模块路径的场景下。</p><pre><code class="javascript">define(function(require, exports) {  console.log(require.resolve(&#39;./b&#39;));  // ==&gt; http://example.com/path/to/b.js});</code></pre></li></ol></li><li><p>exports: exports 是一个对象，用来向外提供模块接口。<br><strong><em>注意</em></strong>：exports 仅仅是 module.exports 的一个引用。在 factory 内部给 exports 重新赋值时，并不会改变 module.exports 的值。因此给 exports 赋值是无效的，不能用来更改模块接口。</p></li><li><p>module: module 是一个对象，上面存储了与当前模块相关联的一些属性和方法。</p><ol><li>module.id <code>String</code> : 模块的唯一标识。</li><li>module.uri <code>String</code> : 根据模块系统的路径解析规则得到的模块绝对路径。一般情况下（没有在 define 中手写 id 参数时），module.id 的值就是 module.uri，两者完全相同。</li><li>module.dependencies <code>Array</code> : dependencies 是一个数组，表示当前模块的依赖。</li><li>module.exports <code>Object</code> : 当前模块对外提供的接口。传给 factory 构造方法的 exports 参数是 module.exports 对象的一个引用。只通过 exports 参数来提供接口，有时无法满足开发者的所有需求。 比如当模块的接口是某个类的实例时，需要通过 module.exports 来实现.</li></ol></li></ul><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>看到这里相信对AMD/CMD有了大概的了解。<br>虽然AMD和CMD蛮相像的，但是它们的区别还是挺明显的。这里引玉伯的话：</p><blockquote><p>作者：玉伯<br>链接：<a href="https://www.zhihu.com/question/20351507/answer/14859415" target="_blank" rel="noopener">AMD 和 CMD 的区别有哪些？</a><br>来源：知乎<br>著作权归作者所有。</p></blockquote><p>我们可以来简单的测试下，创建如下3个js文件：</p><pre><code class="javascript">//mod1.jsdefine(function (require, exports, module) {    console.log(&quot;require mod1&quot;);    return {        hello:function () {            console.log(&quot;hello mod1&quot;);        }    }})</code></pre><pre><code class="javascript">//mod2.jsdefine(function (require, exports, module) {    console.log(&quot;require mod2&quot;);    return {        hello:function () {            console.log(&quot;hello mod2&quot;);        }    }})//main.jsdefine(function(require, exports, module) {    console.log(&#39;require module: main&#39;);    var mod1 = require(&#39;./mod1&#39;);    mod1.hello();    var mod2 = require(&#39;./mod2&#39;);    mod2.hello();    return {        hello: function() {            console.log(&#39;hello main&#39;);        }    };});</code></pre><p>在requirejs中，控制台输出如下：</p><pre><code class="javascript">require mod1require mod2require module: mainhello mod1hello mod2</code></pre><p>在seajs中，控制台输出如下：</p><pre><code class="javascript">require module: mainrequire mod1hello mod1require mod2hello mod2</code></pre><p>OK 经过测试发现：</p><p>SeaJS只会在真正需要使用(依赖)模块时才执行该模块<br>SeaJS是异步加载模块的没错, 但执行模块的顺序也是严格按照模块在代码中出现(require)的顺序</p><p>而RequireJS会先尽早地执行(依赖)模块, 相当于所有的require都被提前了, 而且模块执行的顺序也不一定100%就是先mod1再mod2</p>]]></content>
    
    
    <categories>
      
      <category>规范</category>
      
      <category>模块化</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CommonJS</tag>
      
      <tag>AMD</tag>
      
      <tag>CMD</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
